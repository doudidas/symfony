{% extends 'base.html.twig' %}

{% block body %}

    <div style="padding-bottom:100px"></div>
    <div class="container">
        <h1>
            Promote user
        </h1>
        {% for user in users %}
            <div class="jumbotron">
                <p>
                    <h2>{{user.username}}
                        <small>
                            {{user.roles.0}}</small>
                    </h2>

                    <a href="#">
                        <button type="button" class="btn btn">promote</button>
                    </a>
                </p>
            </div>
        {% endfor %}

    </div>
</div>

{% endblock %}
